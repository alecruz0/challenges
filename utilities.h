#include <stdio.h>

/**
 * Print usage of rotation to user.
 */
void usage();

/**
 * Generates values to a given matrix.
 *
 * @param matrix - matrix to set values to
 * @param rows - number of rows of matrix
 * @param columns - number of columns of matrix
 */
void generate_matrix(int** matrix, int rows, int columns);

